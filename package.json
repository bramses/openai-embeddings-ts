{
  "name": "openai-embeddings",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsc --build && node lib/src/index.js",
    "test": "tsc --build && node lib/test/index.js",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "compute-cosine-similarity": "^1.0.0",
    "csv-writer": "^1.6.0",
    "dotenv": "^10.0.0",
    "gpt-3-encoder": "^1.1.3"
  },
  "devDependencies": {
    "@types/node": "^16.11.12"
  }
}
